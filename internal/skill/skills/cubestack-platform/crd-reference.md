# CubeStack CRD quick reference

Generated by make update-cubestack-skill from the vendored CubeStack CRDs
(test/e2e/framework/testdata/cubestack-crds). Do not edit by hand; the
freshness unit test in internal/skill/cubestackgen fails when this file is
stale.

## DevEnvironment
- resource `devenvironments.ai.cubestack.io`, apiVersion `ai.cubestack.io/v1alpha1`, scope Namespaced
- spec requires: image, resources

- `spec.image` — string · required
- `spec.lifecycle` — object
- `spec.lifecycle.idleTimeout` — integer (int32) · default: 0 · min 0
- `spec.network` — object
- `spec.network.rdmaEnabled` — boolean · default: false
- `spec.network.rdmaType` — string · enum: infiniband, roce · default: roce
- `spec.ports` — array
- `spec.ports[].containerPort` — integer (int32) · required · min 1
- `spec.ports[].name` — string · required
- `spec.ports[].type` — string · enum: http, tcp, udp · default: http
- `spec.resources` — object · required
- `spec.resources.cpu` — string
- `spec.resources.gpuCount` — integer (int32) · default: 1 · min 1
- `spec.resources.gpuType` — string · enum: nvidia, metax · default: nvidia
- `spec.resources.memory` — string
- `spec.running` — boolean · default: false
- `spec.runtime` — object
- `spec.runtime.args` — array
- `spec.runtime.command` — array
- `spec.runtime.env` — array
- `spec.runtime.securityContext` — object
- `spec.ssh` — object
- `spec.ssh.enabled` — boolean · default: false
- `spec.ssh.keysSecret` — object
- `spec.storage` — object
- `spec.storage.mountPath` — string · default: /workspace
- `spec.storage.pvcRetention` — string · enum: retain, delete · default: retain
- `spec.storage.size` — string · default: 10Gi
- `spec.storage.storageClassName` — string
- `spec.type` — string · enum: jupyter, ssh, vscode · default: ssh
- `spec.volumes` — array
- `spec.volumes[].mountPath` — string · required
- `spec.volumes[].name` — string · required
- `spec.volumes[].pvcName` — string · required
- `spec.volumes[].readOnly` — boolean
- `spec.volumes[].subPath` — string

## InferenceRuntimeProfile
- resource `inferenceruntimeprofiles.ai.cubestack.io`, apiVersion `ai.cubestack.io/v1alpha1`, scope Cluster
- spec requires: accelerator, endpoint, engine, modelRequirements, roles

- `spec.accelerator` — object · required
- `spec.accelerator.models` — array · required
- `spec.accelerator.vendor` — string · required · enum: metax, nvidia
- `spec.assets` — array
- `spec.assets[].configMapRef` — object · required
- `spec.assets[].envFrom` — boolean
- `spec.assets[].mount` — object
- `spec.assets[].name` — string · required
- `spec.endpoint` — object · required
- `spec.endpoint.portName` — string · default: http
- `spec.endpoint.role` — string · required
- `spec.engine` — object · required
- `spec.engine.name` — string · required
- `spec.engine.version` — string · required
- `spec.modelRequirements` — object · required
- `spec.modelRequirements.architectures` — array · required
- `spec.modelRequirements.quantization` — array · required
- `spec.overrides` — array
- `spec.overrides[].default` — ?
- `spec.overrides[].description` — string
- `spec.overrides[].enum` — array
- `spec.overrides[].max` — integer (int64)
- `spec.overrides[].min` — integer (int64)
- `spec.overrides[].name` — string · required
- `spec.overrides[].type` — string · required · enum: integer, string, boolean
- `spec.readinessPolicy` — object
- `spec.readinessPolicy.requireAllRoles` — boolean · required · default: true
- `spec.roles` — array · required
- `spec.roles[].dependsOn` — array
- `spec.roles[].name` — string · required
- `spec.roles[].podTemplate` — object · required
- `spec.roles[].service` — object
- `spec.roles[].workload` — object · required
- `spec.vars` — object

## InferenceService
- resource `inferenceservices.ai.cubestack.io`, apiVersion `ai.cubestack.io/v1alpha1`, scope Namespaced
- spec requires: modelRef, profileRef

- `spec.modelRef` — string · required
- `spec.overrides` — object
- `spec.profileRef` — string · required
- `spec.route` — object
- `spec.route.modelName` — string
- `spec.route.publish` — boolean · default: false
- `spec.route.timeoutSeconds` — integer (int64) · default: 60 · min 1

## ModelVersion
- resource `modelversions.ai.cubestack.io`, apiVersion `ai.cubestack.io/v1alpha1`, scope Cluster
- spec requires: architecture, model, quantization, storage, version

- `spec.architecture` — string · required
- `spec.meta` — object
- `spec.model` — string · required
- `spec.quantization` — string · required
- `spec.storage` — object · required
- `spec.storage.hostPath` — object
- `spec.storage.pvc` — object
- `spec.storage.strategy` — string · required · enum: HostPath, PVC
- `spec.version` — string · required

