meta {
  name: delete LLM
  type: http
  seq: 2
}

docs {
  # 删 provider

  删掉整个 provider,连同它服务的**所有** model id。
  只删本 API 为该 provider 命名的那个 Secret(`llm-<name>`)。
  若 `credentialRef` 指向别的 Secret(手工改过 CR、多个 provider 共用一个凭据),
  该 Secret **会被保留**并在响应的 `warning` 里说明。

  (只想去掉某一个 id 用 `5-llm-admin/01-edit-llm` —— 那是 PUT,不碰 Secret。)

  若模板的 `defaultModel` 指向它,一并清空。

  ⚠️ 只要有实例的 `selectedModel` 选中**它服务的任意一个** id → **409**,
  错误体额外带 `instances: [{name, owner}]` —— 这是**唯一**要求客户端解析
  结构化错误体的地方(Portal 把它渲染成可点击的实例列表)。选择是 fail-closed 的。

  ⚠️ 这条会**真的删掉集合正在用的 provider**,跑完记得用 `2-provision/01-add-llm` 加回来。
}

delete {
  url: {{baseUrl}}/api/v1/llms/{{llmName}}
  body: none
  auth: none
}

headers {
  X-CubePilot-User: {{user}}
}

tests {
  test("200 removed, or 409 if a instance selects one of its models", function() {
    expect([200, 409]).to.include(res.status)
  })
}
